---
title: 计算机网络 -- 一次网页请求
date: 2017-06-13
tags: [计算机网络, HTTP]
categories:
  - 计算机基础
  - 计算机网络
---

把前面的内容串起来，看一次网页请求。

不能忘的点：

- 先 DNS，把域名解析成 IP。
- 再 TCP 建立连接，HTTPS 还要做 TLS 握手。
- 然后发送 HTTP 请求，服务器返回响应。

数据真正发出去时，还会经过 TCP 封装、IP 封装、以太网封装。

一句话：  
在浏览器里输入一个网址，背后其实是 DNS、TCP/IP、TLS、HTTP 多层协议一起工作。

