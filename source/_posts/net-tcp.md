---
title: 计算机网络 -- TCP
date: 2017-05-16
tags: [计算机网络, TCP]
categories:
  - 计算机基础
  - 计算机网络
---

TCP 是可靠的、面向连接的传输层协议。

不能忘的点：

- TCP 通过序号和确认号保证可靠传输。
- TCP 把字节流交给应用层，不保留消息边界。
- TCP 同时要处理可靠性、流量控制和拥塞控制。

“面向连接”不是说真的有一根线连着，而是通信双方维护了一组状态。

TCP 的复杂性来自一个目标：在不可靠的 IP 网络上，尽量提供可靠的字节流。

