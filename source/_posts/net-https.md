---
title: 计算机网络 -- HTTPS
date: 2017-06-06
tags: [计算机网络, HTTPS]
categories:
  - 计算机基础
  - 计算机网络
---

HTTPS 可以理解成 HTTP + TLS。

不能忘的点：

- HTTPS 解决加密、身份认证和完整性校验。
- 证书用来证明服务器身份。
- 对称加密快，非对称加密适合交换密钥。

大概过程是：  
先用证书和非对称加密协商出密钥，再用对称加密传输 HTTP 数据。

一句话：  
HTTPS 的重点不是“换了个端口”，而是让通信内容不容易被偷看和篡改。

